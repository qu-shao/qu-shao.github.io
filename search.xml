<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Java访问HashMap的方式</title>
    <url>/2022/03/08/HashMap%E8%AE%BF%E9%97%AE%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="java访问hashmap元素的方式">Java访问HashMap元素的方式</h1>
<p>  HashMap作为最常用的集合之一，可以表示元素间的映射关系，或者用于数据的统计。</p>
<p>  HashMap最多只允许一条记录的键为Null；允许多条记录的值为
Null。（不允许键重复，但允许值重复）</p>
<p>  HashMap不支持线程的同步（任一时刻可以有多个线程同时写HashMap，即线程非安全），可能会导致数据的不一致。如果需要同步，可以用Collections的synchronizedMap()方法使HashMap具有同步的能力，或者使ConcurrentHashMap。</p>
<p>  Hashtable与HashMap类似。不同的是：它不允许记录的键或者值为空；它支持线程的同步（任一时刻只有一个线程能写Hashtable，即线程安全），因此也导致了Hashtable在写入时会比较慢。</p>
<p>  最近在刷算法题中，发现经常需要使用它，所以这里对Java访问HashMap元素的方式进行一个汇总，一方面巩固学习，另一方面方便日后查看使用。</p>
<hr />
<h4
id="第一种方式通过key来访问value">第一种方式：通过Key来访问Value</h4>
<p>  HashMap中内置的方法keySet()可以返回一个hashmap中所有key的set集合，再根据得到的key就可以访问hashmap中对应key的value。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; hm = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span>(String key : hm.keySet())&#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">value</span> <span class="operator">=</span> hm.get(key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h4
id="第二种方式通过map.keyset使用iterator访问key和value">第二种方式：通过Map.keySet()使用iterator访问key和value</h4>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; hm = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">Iterator&lt;String&gt; it = hm.keySet().iterator();</span><br><span class="line"><span class="keyword">while</span>(it.hasNext())&#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> it.next();</span><br><span class="line">    <span class="type">String</span> <span class="variable">value</span> <span class="operator">=</span> hm.get(key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h4
id="第三种方式通过map.entry访问key和value">第三种方式：通过Map.Entry()访问key和value</h4>
<p>  entrySet()方法将返回映射中包含的映射的 Set 视图</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; hm = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span>(Map.Entry&lt;String, String&gt; entry : hm.entrySet())&#123;</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> entry.getKey();</span><br><span class="line">    <span class="type">String</span> <span class="variable">value</span> <span class="operator">=</span> entry.getValue();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h4
id="第四种方式通过map.entryset使用iterator访问key和value">第四种方式：通过Map.entrySet()使用iterator访问key和value</h4>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; hm = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line">Iterator&lt;Map.Entry&lt;String, String&gt;&gt; it = hm.entrySet().iterator();</span><br><span class="line"><span class="keyword">while</span>(it.hasNext())&#123;</span><br><span class="line">    Map.Entry&lt;String, String&gt; entry = it.next();</span><br><span class="line">    <span class="type">String</span> <span class="variable">key</span> <span class="operator">=</span> entry.getKey();</span><br><span class="line">    <span class="type">String</span> <span class="variable">value</span> <span class="operator">=</span> entry.getValue();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h4
id="第五种方式通过map.values遍历所有的value但是不能遍历键key">第五种方式：通过Map.values()遍历所有的value，但是不能遍历键key</h4>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, String&gt; hm = <span class="keyword">new</span> <span class="title class_">HashMap</span>&lt;&gt;();</span><br><span class="line"><span class="keyword">for</span>(String value : hm.values())&#123;</span><br><span class="line">    <span class="comment">/* 处理逻辑 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<p>  上述五种方式中，除了最后一种方式只能获取HashMap的value，其余方式都可以同时获取到key和value；同时，方式一和方式二都是使用keySet()方法先获取到key的集合再访问value，只是方式二在对key的获取上使用了迭代器，类似的方式三和方式四也是这样。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>CNN（卷积神经网络）学习笔记</title>
    <url>/2022/04/10/CNN/</url>
    <content><![CDATA[<h1 id="cnn卷积神经网络学习笔记">CNN(卷积神经网络)学习笔记</h1>
<hr />
<h3 id="基本概念">基本概念</h3>
<p><img data-src="/images/CNN.png" /></p>
<p>​
神经网络的基本组成为输入层、隐藏层和输出层。而卷积神经网络和一般的神经网络的区别在于将隐藏层细分为了卷积层、池化层和激活层，其中卷积层通过卷积核在原始图像上平移提取特征；池化层（一般为最大池化和平均池化）压缩数据和参数的量，减小过拟合，降低网路的复杂程度；激活层用来增加非线性分割的能力。</p>
<p>​
为什么需要卷积神经网络：图片数据由很多的像素点组成，每个像素点在不同通道（一般为RGB，3种通道）的值共同表征一张图片。因此一般来说，一张图片数据的输入为N*N*C（其中N*N为图片的像素点个数，C为通道数），而在现实中，N一般可以达到上千，对于一般的神经网络，假设一层隐藏层的神经元个数为M，对于该层隐藏层，需要的参数个数为N*N*M，这会导致神经网络的参数过多了。我们知道，神经网络的学习能力一般是与神经网络的参数个数正相关的，过多的参数会导致最终模型的学习能力过强，过拟合，同时，过多的参数也会增加计算的复杂度。</p>
<p>​
而卷积神经网络可以大大减少神经网络所需要的参数。卷积神经网络的核心就是卷积核，而卷积核的本质就是一个大小很小的矩阵（三维输入的神经元，长度
* 宽度 *
通道），我们通过将这个矩阵在图像上平移运算提取图像的某种特征。卷积核不同，提取的特征也就不同。</p>
<p>​
对于卷积核，我们必须明确，卷积核的深度应该和输入数据的深度一致（比如3通道的图像，则处理的卷积核深度也为3，因为这一特性，在使用一些api的时候不需要人工地指定该值），其次，卷积核的数量等价于经卷积核运算输出的维度，池化层其实也可以看做特殊的一种卷积核，因为一般经过池化后输出数据的大小会小于输入数据的大小（通过设置步长），为保证不损失太多的信息，通常会选择在池化层增加“卷积核”的数量从而产生更多维度的数据。</p>
<h5 id="图片卷积输出大小公式">图片卷积输出大小公式：</h5>
<p>输入图片大小——W*W</p>
<p>卷积核大小——F*F</p>
<p>步长——S</p>
<p>padding的大小——P <span class="math display">\[
N=(W-F+2*P)/S+1
\]</span> 输出图片大小为N*N</p>
<p>PS:对这些超参数，常见的设置是 F=3，S=1，P=1</p>
<h5 id="卷积层参数个数计算公式">卷积层参数个数计算公式：</h5>
<p>存在bias： <span class="math display">\[
Count=C_{out}*(k_{weight}*k_{height}*C_{in}+1)
\]</span></p>
<hr />
<p>​
卷积和池化相当于做特征工程，最后的全连接层在整个卷积神经网络中起到“分类器”的作用（如果FC层作为最后一层，再加上softmax或者wx+b，则可以分别作为分类或回归的作用，即“分类器”或“回归器”的作用）；如果作为倒数第2，3层的话，FC层的作用是信息融合，增强信息表达。</p>
<hr />
<h3 id="经典案例">经典案例</h3>
<h5 id="lenet">LeNet</h5>
<p>LeNet神经网络由深度学习三巨头之一的Yan
LeCun提出，他同时也是卷积神经网络 (CNN，Convolutional Neural
Networks)之父。LeNet主要用来进行手写字符的识别与分类，并在美国的银行中投入了使用。LeNet的实现确立了CNN的结构，现在神经网络中的许多内容在LeNet的网络结构中都能看到，例如卷积层，Pooling层，ReLU层。虽然LeNet早在20世纪90年代就已经提出了，但由于当时缺乏大规模的训练数据，计算机硬件的性能也较低，因此LeNet神经网络在处理复杂问题时效果并不理想。LeNet的设计较为简单，其处理复杂数据的能力有限；此外，在近年来的研究中许多学者已经发现全连接层的计算代价过大，而使用全部由卷积层组成的神经网络。</p>
<h6 id="模型结构">模型结构：</h6>
<p><img data-src="/images/LeNet.jpeg" /></p>
<p>LeNet-5包含七层，不包括输入，每一层都包含可训练参数（权重)，当时使用的输入数据是32*32像素的图像。下面逐层介绍LeNet-5的结构，并且，卷积层将用Cx表示，子采样层则被标记为Sx，完全连接层被标记为Fx，其中x是层索引。</p>
<p>层C1是具有六个5*5的卷积核的卷积层（convolution），特征映射的大小为28*28，这样可以防止输入图像的信息掉出卷积核边界。C1包含156个可训练参数和122304个连接。</p>
<p>层S2是输出6个大小为14*14的特征图的子采样层（subsampling/pooling）。每个特征图中的每个单元连接到C1中的对应特征图中的2*2个邻域。S2中单位的四个输入相加，然后乘以可训练系数（权重），然后加到可训练偏差（bias）。结果通过S形函数传递。由于2*2个感受域不重叠，因此S2中的特征图只有C1中的特征图的一半行数和列数。S2层有12个可训练参数和5880个连接。</p>
<p>层C3是具有16个5*5的卷积核的卷积层。前六个特征图的输入是S2中的三个特征图的每个连续子集，接下来的六个特征图的输入则来自四个连续子集的输入，接下来的三个特征图的输入来自不连续的四个子集。最后一个特征图的输入来自S2所有特征图。C3层有1516个可训练参数和151600个连接。</p>
<p>层S4是与S2类似，大小为2*2，输出为16个5*5的特征图。S4层有32个可训练参数和2000个连接。</p>
<p>层C5是具有120个大小为5*5的卷积核的卷积层。每个单元连接到S4的所有16个特征图上的5*5邻域。这里，因为S4的特征图大小也是5*5，所以C5的输出大小是1*1。因此S4和C5之间是完全连接的。C5被标记为卷积层，而不是完全连接的层，是因为如果LeNet-5输入变得更大而其结构保持不变，则其输出大小会大于1*1，即不是完全连接的层了。C5层有48120个可训练连接。</p>
<p>F6层完全连接到C5，输出84张特征图。它有10164个可训练参数。这里84与输出层的设计有关。</p>
<h6 id="各层参数变化">各层参数变化：</h6>
<p>C1 输入大小：32*32 核大小：5*5 核数目：6 输出大小：28*28*6
训练参数数目：(5*5+1)*6=156
连接数：(5*5+1)*6*(32-2-2)*(32-2-2)=122304</p>
<p>S2 输入大小：28*28*6 核大小：2*2 核数目：1 输出大小：14*14*6
训练参数数目：2*6=12，2=(w,b) 连接数：(2*2+1)*1*14*14*6 = 5880</p>
<p>C3 输入大小：14*14*6 核大小：5*5 核数目：16 输出大小：10*10*16
训练参数数目：6*(3*5*5+1) + 6*(4*5*5+1) + 3*(4*5*5+1) + 1*(6*5*5+1)=1516
连接数：(6*(3*5*5+1) + 6*(4*5*5+1) + 3*(4*5*5+1) +
1*(6*5*5+1))*10*10=151600</p>
<p>S4 输入大小：10*10*16 核大小：2*2 核数目：1 输出大小：5*5*16
训练参数数目：2*16=32 连接数：(2*2+1)*1*5*5*16=2000</p>
<p>C5 输入大小：5*5*16 核大小：5*5 核数目：120 输出大小：120*1*1
训练参数数目：(5*5*16+1)*120*1*1=48120（因为是全连接）
连接数：(5*5*16+1)*120*1*1=48120</p>
<p>F6 输入大小：120 输出大小：84 训练参数数目：(120+1)*84=10164
连接数：(120+1)*84=10164</p>
<h6 id="实验代码">实验代码：</h6>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导入数据</span></span><br><span class="line">mnist = tf.keras.datasets.mnist</span><br><span class="line">(x_train, y_train), (x_test, y_test) = mnist.load_data()</span><br><span class="line"><span class="comment"># 将图片从28*28扩展成32*32</span></span><br><span class="line">X_train = np.pad(x_train, ((<span class="number">0</span>, <span class="number">0</span>), (<span class="number">2</span>, <span class="number">2</span>), (<span class="number">2</span>, <span class="number">2</span>)), <span class="string">&#x27;constant&#x27;</span>, constant_values=<span class="number">0</span>)</span><br><span class="line">X_test = np.pad(x_test, ((<span class="number">0</span>, <span class="number">0</span>), (<span class="number">2</span>, <span class="number">2</span>), (<span class="number">2</span>, <span class="number">2</span>)), <span class="string">&#x27;constant&#x27;</span>, constant_values=<span class="number">0</span>)</span><br><span class="line">X_train = X_train.astype(<span class="string">&#x27;float32&#x27;</span>)</span><br><span class="line">X_test = X_test.astype(<span class="string">&#x27;float32&#x27;</span>)</span><br><span class="line"><span class="comment"># 数据正则化</span></span><br><span class="line">X_train /= <span class="number">255</span></span><br><span class="line">X_test /= <span class="number">255</span></span><br><span class="line"><span class="comment"># 数据维度转换</span></span><br><span class="line">X_train = X_train.reshape(X_train.shape[<span class="number">0</span>], <span class="number">32</span>, <span class="number">32</span>, <span class="number">1</span>)</span><br><span class="line">X_test = X_test.reshape(X_test.shape[<span class="number">0</span>], <span class="number">32</span>, <span class="number">32</span>, <span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">model = tf.keras.Sequential()</span><br><span class="line"><span class="comment"># 卷积层，6 个 5x5 卷积核，步长为 1，relu 激活，第一层需指定 input_shape</span></span><br><span class="line">model.add(tf.keras.layers.Conv2D(filters=<span class="number">6</span>, kernel_size=(<span class="number">5</span>, <span class="number">5</span>), strides=(<span class="number">1</span>, <span class="number">1</span>),</span><br><span class="line">                                 activation=<span class="string">&#x27;relu&#x27;</span>, input_shape=(<span class="number">32</span>, <span class="number">32</span>, <span class="number">1</span>)))</span><br><span class="line"><span class="comment"># 平均池化，池化窗口默认为 2</span></span><br><span class="line">model.add(tf.keras.layers.AveragePooling2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>), strides=<span class="number">2</span>))</span><br><span class="line"><span class="comment"># 卷积层，16 个 5x5 卷积核，步为 1，relu 激活</span></span><br><span class="line">model.add(tf.keras.layers.Conv2D(filters=<span class="number">16</span>, kernel_size=(</span><br><span class="line">    <span class="number">5</span>, <span class="number">5</span>), strides=(<span class="number">1</span>, <span class="number">1</span>), activation=<span class="string">&#x27;relu&#x27;</span>))</span><br><span class="line"><span class="comment"># 平均池化，池化窗口默认为 2</span></span><br><span class="line">model.add(tf.keras.layers.AveragePooling2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>), strides=<span class="number">2</span>))</span><br><span class="line"><span class="comment"># 需展平后才能与全连接层相连</span></span><br><span class="line">model.add(tf.keras.layers.Flatten())</span><br><span class="line"><span class="comment"># 全连接层，输出为 120，relu 激活</span></span><br><span class="line">model.add(tf.keras.layers.Dense(units=<span class="number">120</span>, activation=<span class="string">&#x27;relu&#x27;</span>))</span><br><span class="line"><span class="comment"># 全连接层，输出为 84，relu 激活</span></span><br><span class="line">model.add(tf.keras.layers.Dense(units=<span class="number">84</span>, activation=<span class="string">&#x27;relu&#x27;</span>))</span><br><span class="line"><span class="comment"># 全连接层，输出为 10，Softmax 激活</span></span><br><span class="line">model.add(tf.keras.layers.Dense(units=<span class="number">10</span>, activation=<span class="string">&#x27;softmax&#x27;</span>))</span><br><span class="line"><span class="comment"># 查看网络结构</span></span><br><span class="line">model.summary()</span><br><span class="line"><span class="comment"># 超参数设置</span></span><br><span class="line">num_epochs = <span class="number">10</span></span><br><span class="line">batch_size = <span class="number">64</span></span><br><span class="line">learning_rate = <span class="number">0.01</span></span><br><span class="line"><span class="comment"># 优化器</span></span><br><span class="line">adam_optimizer = tf.keras.optimizers.Adam(learning_rate)</span><br><span class="line"><span class="comment"># 编译</span></span><br><span class="line">model.<span class="built_in">compile</span>(optimizer=adam_optimizer,</span><br><span class="line">              loss=tf.keras.losses.sparse_categorical_crossentropy,</span><br><span class="line">              metrics=[<span class="string">&#x27;accuracy&#x27;</span>])</span><br><span class="line">training = model.fit(x=X_train,</span><br><span class="line">                     y=y_train,</span><br><span class="line">                     batch_size=batch_size,</span><br><span class="line">                     epochs=num_epochs,</span><br><span class="line">                     validation_split=<span class="number">0.20</span>)</span><br><span class="line"><span class="built_in">print</span>(model.evaluate(X_test, y_test))</span><br><span class="line"><span class="comment"># 保存模型</span></span><br><span class="line">model.save(<span class="string">&quot;LeNet_model&quot;</span>, save_format=<span class="string">&quot;tf&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示训练过程</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">show_process</span>(<span class="params">training, train, validation</span>):</span><br><span class="line">    plt.plot(training.history[train], linestyle=<span class="string">&quot;-&quot;</span>, color=<span class="string">&quot;b&quot;</span>)</span><br><span class="line">    plt.plot(training.history[validation], linestyle=<span class="string">&quot;--&quot;</span>, color=<span class="string">&quot;r&quot;</span>)</span><br><span class="line">    plt.title(<span class="string">&quot;training_history&quot;</span>)</span><br><span class="line">    plt.xlabel(<span class="string">&quot;epoch&quot;</span>)</span><br><span class="line">    plt.ylabel(train)</span><br><span class="line">    plt.legend([<span class="string">&quot;train&quot;</span>, <span class="string">&quot;validation&quot;</span>], loc=<span class="string">&quot;lower right&quot;</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">show_process(training, <span class="string">&quot;accuracy&quot;</span>, <span class="string">&quot;val_accuracy&quot;</span>)</span><br><span class="line">show_process(training, <span class="string">&quot;loss&quot;</span>, <span class="string">&quot;val_loss&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用模型</span></span><br><span class="line">predict_x = model.predict(X_test)</span><br><span class="line">prediction = np.argmax(predict_x, axis=<span class="number">1</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">    index = np.random.randint(<span class="number">0</span>, <span class="number">10000</span>)</span><br><span class="line">    plt.subplot(<span class="number">1</span>, <span class="number">4</span>, i + <span class="number">1</span>)</span><br><span class="line">    plt.axis(<span class="string">&quot;off&quot;</span>)</span><br><span class="line">    plt.imshow(x_test[index], cmap=<span class="string">&quot;gray&quot;</span>)</span><br><span class="line">    plt.title(<span class="string">&quot;y=&quot;</span> + <span class="built_in">str</span>(y_test[index]) + <span class="string">&quot;\ny_pred=&quot;</span> + <span class="built_in">str</span>(prediction[index]))</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<h6 id="实验结果">实验结果：</h6>
<p><img data-src="/images/LeNet_acc.png" /></p>
<p><img data-src="/images/LeNet_loss.png" /></p>
<p><img data-src="/images/LeNet_res.png" /></p>
<hr />
<h5 id="alexnet">AlexNet</h5>
<p>AlexNet由Geoffrey和他的学生Alex提出，并在2012年的ILSVRC竞赛中获得了第一名。Alexnet共有8层结构，前5层为卷积层，后三层为全连接层。AlexNet网络结构具有如下特点：</p>
<ol type="1">
<li>AlexNet在激活函数上选取了非线性非饱和的relu函数，在训练阶段梯度衰减快慢方面，relu函数比传统神经网络所选取的非线性饱和函数（如sigmoid函数，tanh函数）要快许多。</li>
<li>AlexNet在双gpu上运行，每个gpu负责一半网络的运算。</li>
<li>采用局部响应归一化（LRN）。对于非饱和函数relu来说，不需要对其输入进行标准化，但Alex等人发现，在relu层加入LRN，可形成某种形式的横向抑制，从而提高网络的泛华能力。</li>
<li>池化方式采用overlapping
pooling。即池化窗口的大小大于步长，使得每次池化都有重叠的部分。（ps:这种重叠的池化方式比传统无重叠的池化方式有着更好的效果，且可以避免过拟合现象的发生）</li>
<li>使用随机丢弃技术（dropout）选择性地忽略训练中的单个神经元，避免模型的过拟合。</li>
</ol>
<h6 id="模型结构-1">模型结构：</h6>
<p><img data-src="/images/AlexNet.png" /></p>
<p>AlexNet一共有8层</p>
<p>第一层：卷积层1，输入为 224 × 224 × 3
的图像，卷积核的数量为96，论文中两片GPU分别计算48个核; 卷积核的大小为 11
× 11 × 3，stride = 4，padding = 2。然后进行 (Local Response Normalized),
后面跟着池化pool_size = (3, 3), stride = 2, padding = 0
最终获得第一层卷积的feature map，最终第一层卷积的输出为96×55×55；</p>
<p>第二层：卷积层2，输入为上一层卷积的feature map，
卷积核的个数为256个，论文中的两个GPU分别有128个卷积核。卷积核的大小为：5
× 5 × 48, padding = 2, stride = 1; 然后做 LRN， 最后 max_pooling,
pool_size = (3, 3), stride = 2；</p>
<p>第三层：卷积层3，输入为第二层的输出，卷积核个数为384, kernel_size =
(3 × 3 × 256 )， padding = 1，第三层没有做LRN和Pool；</p>
<p>第四层：卷积层4，输入为第三层的输出，卷积核个数为384, kernel_size =
(3 × 3 ), padding = 1，和第三层一样，没有LRN和Pool；</p>
<p>第五层：卷积层5，输入为第四层的输出，卷积核个数为256, kernel_size =
(3 × 3 ), padding = 1。然后直接进行max_pooling, pool_size = (3, 3),
stride = 2；</p>
<p>第6，7，8层是全连接层，每一层的神经元的个数为4096，最终输出softmax为1000（ImageNet这个比赛的分类个数为1000）。全连接层中使用了RELU和Dropout。</p>
<h6 id="实验代码-1">实验代码：</h6>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> keras.datasets <span class="keyword">import</span> mnist</span><br><span class="line"><span class="keyword">from</span> keras.utils <span class="keyword">import</span> np_utils</span><br><span class="line"><span class="keyword">from</span> keras.layers <span class="keyword">import</span> Dense, Dropout, Flatten, Conv2D, MaxPool2D, BatchNormalization</span><br><span class="line"><span class="keyword">from</span> keras.models <span class="keyword">import</span> Sequential</span><br><span class="line"><span class="keyword">from</span> matplotlib <span class="keyword">import</span> pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line">os.environ[<span class="string">&#x27;KMP_DUPLICATE_LIB_OK&#x27;</span>] = <span class="string">&#x27;True&#x27;</span></span><br><span class="line">(X_train, Y_train), (X_test, Y_test) = mnist.load_data()</span><br><span class="line">X_test1 = X_test</span><br><span class="line">Y_test1 = Y_test</span><br><span class="line"></span><br><span class="line"><span class="comment"># 处理图像特征</span></span><br><span class="line">X_train = X_train.reshape(-<span class="number">1</span>, <span class="number">28</span>, <span class="number">28</span>, <span class="number">1</span>).astype(<span class="string">&quot;float32&quot;</span>) / <span class="number">255.0</span></span><br><span class="line">X_test = X_test.reshape(-<span class="number">1</span>, <span class="number">28</span>, <span class="number">28</span>, <span class="number">1</span>).astype(<span class="string">&quot;float32&quot;</span>) / <span class="number">255.0</span></span><br><span class="line"><span class="comment"># 处理标签</span></span><br><span class="line">Y_train = np_utils.to_categorical(Y_train, <span class="number">10</span>)</span><br><span class="line">Y_test = np_utils.to_categorical(Y_test, <span class="number">10</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搭建AlexNet网络模型</span></span><br><span class="line"><span class="comment"># 建立第一层卷积</span></span><br><span class="line">model = Sequential()</span><br><span class="line">model.add(Conv2D(</span><br><span class="line">    filters=<span class="number">96</span>,</span><br><span class="line">    kernel_size=(<span class="number">11</span>, <span class="number">11</span>),</span><br><span class="line">    strides=<span class="number">4</span>,</span><br><span class="line">    padding=<span class="string">&quot;same&quot;</span>,</span><br><span class="line">    input_shape=(<span class="number">28</span>, <span class="number">28</span>, <span class="number">1</span>),</span><br><span class="line">    activation=<span class="string">&quot;relu&quot;</span></span><br><span class="line">))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搭建BN层</span></span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line"><span class="comment"># 搭建第一层重叠最大池化层</span></span><br><span class="line">model.add(MaxPool2D(</span><br><span class="line">    pool_size=(<span class="number">3</span>, <span class="number">3</span>),</span><br><span class="line">    strides=<span class="number">2</span>,</span><br><span class="line">    padding=<span class="string">&quot;same&quot;</span></span><br><span class="line">))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 建立第二层卷积</span></span><br><span class="line">model.add(Conv2D(</span><br><span class="line">    filters=<span class="number">256</span>,</span><br><span class="line">    kernel_size=(<span class="number">5</span>, <span class="number">5</span>),</span><br><span class="line">    strides=<span class="number">1</span>,</span><br><span class="line">    padding=<span class="string">&quot;same&quot;</span>,</span><br><span class="line">    activation=<span class="string">&quot;relu&quot;</span></span><br><span class="line">))</span><br><span class="line"><span class="comment"># 搭建BN层</span></span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line"><span class="comment"># 搭建第二层池化层</span></span><br><span class="line">model.add(MaxPool2D(</span><br><span class="line">    pool_size=(<span class="number">3</span>, <span class="number">3</span>),</span><br><span class="line">    strides=<span class="number">2</span>,</span><br><span class="line">    padding=<span class="string">&quot;same&quot;</span>,</span><br><span class="line">))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搭建第三层卷积</span></span><br><span class="line">model.add(Conv2D(</span><br><span class="line">    filters=<span class="number">384</span>,</span><br><span class="line">    kernel_size=(<span class="number">3</span>, <span class="number">3</span>),</span><br><span class="line">    strides=<span class="number">1</span>,</span><br><span class="line">    padding=<span class="string">&quot;same&quot;</span>,</span><br><span class="line">    activation=<span class="string">&quot;relu&quot;</span>,</span><br><span class="line">))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搭建第四层卷积</span></span><br><span class="line">model.add(Conv2D(</span><br><span class="line">    filters=<span class="number">384</span>,</span><br><span class="line">    kernel_size=(<span class="number">3</span>, <span class="number">3</span>),</span><br><span class="line">    strides=<span class="number">1</span>,</span><br><span class="line">    padding=<span class="string">&quot;same&quot;</span>,</span><br><span class="line">    activation=<span class="string">&quot;relu&quot;</span></span><br><span class="line">))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搭建第五卷积层</span></span><br><span class="line">model.add(Conv2D(</span><br><span class="line">    filters=<span class="number">256</span>,</span><br><span class="line">    kernel_size=(<span class="number">3</span>, <span class="number">3</span>),</span><br><span class="line">    strides=<span class="number">1</span>,</span><br><span class="line">    padding=<span class="string">&#x27;same&#x27;</span>,</span><br><span class="line">    activation=<span class="string">&quot;relu&quot;</span></span><br><span class="line">))</span><br><span class="line">model.add(MaxPool2D(</span><br><span class="line">    pool_size=(<span class="number">3</span>, <span class="number">3</span>),</span><br><span class="line">    strides=<span class="number">2</span>,</span><br><span class="line">    padding=<span class="string">&quot;same&quot;</span></span><br><span class="line">))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 搭建第六层：全连接层</span></span><br><span class="line"><span class="comment"># 在搭建全连接层之前，必须使用Flatten()降维</span></span><br><span class="line">model.add(Flatten())</span><br><span class="line"><span class="comment"># 全连接层</span></span><br><span class="line">model.add(Dense(<span class="number">4096</span>, activation=<span class="string">&quot;relu&quot;</span>))</span><br><span class="line">model.add(Dropout(<span class="number">0.5</span>))</span><br><span class="line"><span class="comment"># 搭建第七层：全连接层</span></span><br><span class="line">model.add(Dense(<span class="number">2048</span>, activation=<span class="string">&quot;relu&quot;</span>))</span><br><span class="line">model.add(Dropout(<span class="number">0.5</span>))</span><br><span class="line"><span class="comment"># 搭建第八层：全连接层即输出层</span></span><br><span class="line">model.add(Dense(<span class="number">10</span>, activation=<span class="string">&quot;softmax&quot;</span>))</span><br><span class="line">model.summary()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 编译</span></span><br><span class="line">model.<span class="built_in">compile</span>(</span><br><span class="line">    loss=<span class="string">&quot;categorical_crossentropy&quot;</span>,</span><br><span class="line">    optimizer=<span class="string">&quot;adam&quot;</span>,</span><br><span class="line">    metrics=[<span class="string">&quot;accuracy&quot;</span>]</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 训练</span></span><br><span class="line">n_epoch = <span class="number">10</span></span><br><span class="line">n_batch = <span class="number">256</span></span><br><span class="line">training = model.fit(</span><br><span class="line">    X_train,</span><br><span class="line">    Y_train,</span><br><span class="line">    epochs=n_epoch,</span><br><span class="line">    batch_size=n_batch,</span><br><span class="line">    verbose=<span class="number">1</span>,</span><br><span class="line">    validation_split=<span class="number">0.20</span></span><br><span class="line">)</span><br><span class="line"><span class="comment"># 评估</span></span><br><span class="line"><span class="built_in">print</span>(model.evaluate(X_train, Y_train, verbose=<span class="number">1</span>))</span><br><span class="line"><span class="comment"># 保存模型</span></span><br><span class="line">model.save(<span class="string">&quot;AlexNet_model&quot;</span>, save_format=<span class="string">&quot;tf&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示训练过程</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">show_process</span>(<span class="params">training, train, validation</span>):</span><br><span class="line">    plt.plot(training.history[train], linestyle=<span class="string">&quot;-&quot;</span>, color=<span class="string">&quot;b&quot;</span>)</span><br><span class="line">    plt.plot(training.history[validation], linestyle=<span class="string">&quot;--&quot;</span>, color=<span class="string">&quot;r&quot;</span>)</span><br><span class="line">    plt.title(<span class="string">&quot;training_history&quot;</span>)</span><br><span class="line">    plt.xlabel(<span class="string">&quot;epoch&quot;</span>)</span><br><span class="line">    plt.ylabel(train)</span><br><span class="line">    plt.legend([<span class="string">&quot;train&quot;</span>, <span class="string">&quot;validation&quot;</span>], loc=<span class="string">&quot;lower right&quot;</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">show_process(training, <span class="string">&quot;accuracy&quot;</span>, <span class="string">&quot;val_accuracy&quot;</span>)</span><br><span class="line">show_process(training, <span class="string">&quot;loss&quot;</span>, <span class="string">&quot;val_loss&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 预测</span></span><br><span class="line">predict_x = model.predict(X_test)</span><br><span class="line">prediction = np.argmax(predict_x, axis=<span class="number">1</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">3</span>):</span><br><span class="line">    index = np.random.randint(<span class="number">0</span>, <span class="number">10000</span>)</span><br><span class="line">    plt.subplot(<span class="number">1</span>, <span class="number">3</span>, i + <span class="number">1</span>)</span><br><span class="line">    plt.axis(<span class="string">&quot;off&quot;</span>)</span><br><span class="line">    plt.imshow(X_test1[index])</span><br><span class="line">    plt.title(<span class="string">&quot;y=&quot;</span> + <span class="built_in">str</span>(Y_test1[index]) + <span class="string">&quot;\ny_pred=&quot;</span> + <span class="built_in">str</span>(prediction[index]))</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<h6 id="实验结果-1">实验结果：</h6>
<p><img data-src="/images/AlexNet_acc.png" /></p>
<p><img data-src="/images/AlexNet_loss.png" /></p>
<p><img data-src="/images/AlexNet_res.png" /></p>
<hr />
<h5 id="vggnet">VGGNet</h5>
<p>2014年，牛津大学计算机视觉组和Google
DeepMind公司的研究员一起研发出了新的深度卷积神经网络：VGGNet，并取得了ILSVRC2014比赛分类项目的第二名（第一名是GoogLeNet，也是同年提出的）和定位项目的第一名。</p>
<p>VGGNet探索了卷积神经网络的深度与其性能之间的关系，成功地构筑了16~19层深的卷积神经网络，证明了增加网络的深度能够在一定程度上影响网络最终的性能，使错误率大幅下降，同时拓展性又很强，迁移到其它图片数据上的泛化性也非常好。到目前为止，VGG仍然被用来提取图像特征。VGGNet可以看成是加深版本的AlexNet，都是由卷积层、全连接层两大部分构成。它有如下几个特点：</p>
<ol type="1">
<li>小卷积核和多卷积子层，使用小卷积核(3x3)和多卷积子层代替一个卷积核较大的卷积层的好处一是可以减少参数，二是相当于进行了更多的非线性映射，可以增加网络的拟合/表达能力。VGG的作者认为两个3x3的卷积堆叠获得的感受野大小，相当一个5x5的卷积；而3个3x3卷积的堆叠获取到的感受野相当于一个7x7的卷积。这样可以增加非线性映射，也能很好地减少参数（例如7x7的参数为49个，而3个3x3的参数为27）</li>
<li>小池化核，相比AlexNet的3x3的池化核，VGG全部采用2x2的池化核。</li>
<li>通道数多，VGG网络第一层的通道数为64，后面每层都进行了翻倍，最多到512个通道，通道数的增加，使得更多的信息可以被提取出来。</li>
<li>层数更深、特征图更宽，由于卷积核专注于扩大通道数、池化专注于缩小宽和高，使得模型架构上更深更宽的同时，控制了计算量的增加规模。</li>
<li>全连接转卷积（测试阶段），在网络测试阶段将训练阶段的三个全连接替换为三个卷积，使得测试得到的全卷积网络因为没有全连接的限制，因而可以接收任意宽或高为的输入，这在测试阶段很重要。</li>
<li>用简单模型初始化复杂模型，VGGNet在训练的时候先训级别A的简单模型，再复用A网络的权重来初始化后面的几个复杂模型，这样收敛速度更快。</li>
<li>采用了Multi-Scale的方法来训练和预测，可以增加训练的数据量，防止模型过拟合，提升预测准确率。</li>
</ol>
<h6 id="模型结构-2">模型结构：</h6>
<p><img data-src="/images/VGGNet.png" /></p>
<p>VGG16处理过程如下：</p>
<ol type="1">
<li>输入224x224x3的图片，经64个3x3的卷积核做两次卷积+ReLU，卷积后的尺寸变为224x224x64</li>
<li>做max
pooling（最大化池化），池化单元尺寸为2x2（效果为图像尺寸减半），池化后的尺寸变为112x112x64</li>
<li>经128个3x3的卷积核做两次卷积+ReLU，尺寸变为112x112x128</li>
<li>做2x2的max pooling池化，尺寸变为56x56x128</li>
<li>经256个3x3的卷积核做三次卷积+ReLU，尺寸变为56x56x256</li>
<li>做2x2的max pooling池化，尺寸变为28x28x256</li>
<li>经512个3x3的卷积核做三次卷积+ReLU，尺寸变为28x28x512</li>
<li>做2x2的max pooling池化，尺寸变为14x14x512</li>
<li>经512个3x3的卷积核做三次卷积+ReLU，尺寸变为14x14x512</li>
<li>做2x2的max pooling池化，尺寸变为7x7x512</li>
<li>与两层1x1x4096，一层1x1x1000进行全连接+ReLU（共三层）</li>
<li>通过softmax输出1000个预测结果</li>
</ol>
<h6 id="实验代码-2">实验代码：</h6>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> keras.datasets <span class="keyword">import</span> cifar10</span><br><span class="line"><span class="keyword">from</span> keras.utils <span class="keyword">import</span> np_utils</span><br><span class="line"><span class="keyword">from</span> keras.layers <span class="keyword">import</span> Dense, Dropout, Flatten, Conv2D, MaxPool2D, BatchNormalization</span><br><span class="line"><span class="keyword">from</span> keras.models <span class="keyword">import</span> Sequential</span><br><span class="line"><span class="keyword">from</span> matplotlib <span class="keyword">import</span> pyplot <span class="keyword">as</span> plt</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> datetime</span><br><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">os.environ[<span class="string">&#x27;KMP_DUPLICATE_LIB_OK&#x27;</span>] = <span class="string">&quot;True&quot;</span></span><br><span class="line"><span class="comment"># 获取所有GPU组成list</span></span><br><span class="line">gpus = tf.config.experimental.list_physical_devices(<span class="string">&#x27;GPU&#x27;</span>)</span><br><span class="line"><span class="comment"># 设置按需申请</span></span><br><span class="line"><span class="comment"># 由于我这里仅有一块GPU,multi-GPU需要for一下</span></span><br><span class="line">tf.config.experimental.set_memory_growth(gpus[<span class="number">0</span>], <span class="literal">True</span>)</span><br><span class="line"></span><br><span class="line">(X_train, Y_train), (X_test, Y_test) = cifar10.load_data()</span><br><span class="line">X_test1 = X_test</span><br><span class="line">Y_test1 = Y_test</span><br><span class="line"></span><br><span class="line"><span class="comment"># 处理图像特征</span></span><br><span class="line">X_train = X_train.reshape(-<span class="number">1</span>, <span class="number">32</span>, <span class="number">32</span>, <span class="number">3</span>).astype(<span class="string">&quot;float32&quot;</span>) / <span class="number">255.0</span></span><br><span class="line">X_test = X_test.reshape(-<span class="number">1</span>, <span class="number">32</span>, <span class="number">32</span>, <span class="number">3</span>).astype(<span class="string">&quot;float32&quot;</span>) / <span class="number">255.0</span></span><br><span class="line"><span class="comment"># 处理标签</span></span><br><span class="line">Y_train = np_utils.to_categorical(Y_train, <span class="number">10</span>)</span><br><span class="line">Y_test = np_utils.to_categorical(Y_test, <span class="number">10</span>)</span><br><span class="line"></span><br><span class="line">model = Sequential()</span><br><span class="line">model.add(Conv2D(<span class="number">64</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>, input_shape=(<span class="number">32</span>, <span class="number">32</span>, <span class="number">3</span>)))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">64</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(MaxPool2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>), padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line"></span><br><span class="line">model.add(Conv2D(<span class="number">128</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">128</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(MaxPool2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>), padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line"></span><br><span class="line">model.add(Conv2D(<span class="number">256</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">256</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">256</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(MaxPool2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>), padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line"></span><br><span class="line">model.add(Conv2D(<span class="number">512</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">512</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">512</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(MaxPool2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>), padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line"></span><br><span class="line">model.add(Conv2D(<span class="number">512</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">512</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(Conv2D(<span class="number">512</span>, (<span class="number">3</span>, <span class="number">3</span>), activation=<span class="string">&#x27;relu&#x27;</span>, padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line">model.add(BatchNormalization())</span><br><span class="line">model.add(MaxPool2D(pool_size=(<span class="number">2</span>, <span class="number">2</span>), padding=<span class="string">&#x27;same&#x27;</span>))</span><br><span class="line"></span><br><span class="line">model.add(Flatten())</span><br><span class="line">model.add(Dense(<span class="number">4096</span>, activation=<span class="string">&quot;relu&quot;</span>))</span><br><span class="line">model.add(Dropout(<span class="number">0.5</span>))</span><br><span class="line">model.add(Dense(<span class="number">4096</span>, activation=<span class="string">&quot;relu&quot;</span>))</span><br><span class="line">model.add(Dropout(<span class="number">0.5</span>))</span><br><span class="line">model.add(Dense(<span class="number">10</span>, activation=<span class="string">&quot;softmax&quot;</span>))</span><br><span class="line">model.summary()</span><br><span class="line"></span><br><span class="line">model.<span class="built_in">compile</span>(loss=<span class="string">&quot;categorical_crossentropy&quot;</span>, optimizer=<span class="string">&#x27;adam&#x27;</span>, metrics=[<span class="string">&#x27;accuracy&#x27;</span>])</span><br><span class="line"></span><br><span class="line">log_dir = <span class="string">&quot;logs/fit/&quot;</span> + datetime.datetime.now().strftime(<span class="string">&quot;%Y%m%d-%H%M%S&quot;</span>)</span><br><span class="line">tensorboard_callback = tf.keras.callbacks.TensorBoard(log_dir=log_dir, histogram_freq=<span class="number">1</span>)</span><br><span class="line">n_epoch = <span class="number">1000</span></span><br><span class="line">n_batch = <span class="number">512</span></span><br><span class="line">training = model.fit(</span><br><span class="line">    X_train,</span><br><span class="line">    Y_train,</span><br><span class="line">    epochs=n_epoch,</span><br><span class="line">    batch_size=n_batch,</span><br><span class="line">    verbose=<span class="number">1</span>,</span><br><span class="line">    validation_split=<span class="number">0.20</span>,</span><br><span class="line">    callbacks=[tensorboard_callback]</span><br><span class="line">)</span><br><span class="line"><span class="built_in">print</span>(model.evaluate(X_test, Y_test))</span><br><span class="line"><span class="comment"># 保存模型</span></span><br><span class="line">model.save(<span class="string">&quot;VGGNet16_model&quot;</span>, save_format=<span class="string">&quot;tf&quot;</span>)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 显示训练过程</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">show_process</span>(<span class="params">training, train, validation</span>):</span><br><span class="line">    plt.plot(training.history[train], linestyle=<span class="string">&quot;-&quot;</span>, color=<span class="string">&quot;b&quot;</span>)</span><br><span class="line">    plt.plot(training.history[validation], linestyle=<span class="string">&quot;--&quot;</span>, color=<span class="string">&quot;r&quot;</span>)</span><br><span class="line">    plt.title(<span class="string">&quot;training_history&quot;</span>)</span><br><span class="line">    plt.xlabel(<span class="string">&quot;epoch&quot;</span>)</span><br><span class="line">    plt.ylabel(train)</span><br><span class="line">    plt.legend([<span class="string">&quot;train&quot;</span>, <span class="string">&quot;validation&quot;</span>], loc=<span class="string">&quot;lower right&quot;</span>)</span><br><span class="line">    plt.show()</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">show_process(training, <span class="string">&quot;accuracy&quot;</span>, <span class="string">&quot;val_accuracy&quot;</span>)</span><br><span class="line">show_process(training, <span class="string">&quot;loss&quot;</span>, <span class="string">&quot;val_loss&quot;</span>)</span><br><span class="line"></span><br><span class="line">class_names = [<span class="string">&#x27;airplane&#x27;</span>, <span class="string">&#x27;automobile&#x27;</span>, <span class="string">&#x27;bird&#x27;</span>, <span class="string">&#x27;cat&#x27;</span>, <span class="string">&#x27;deer&#x27;</span>, <span class="string">&#x27;dog&#x27;</span>, <span class="string">&#x27;frog&#x27;</span>, <span class="string">&#x27;horse&#x27;</span>, <span class="string">&#x27;ship&#x27;</span>, <span class="string">&#x27;truck&#x27;</span>]</span><br><span class="line">predict_x = model.predict(X_test)</span><br><span class="line">prediction = np.argmax(predict_x, axis=<span class="number">1</span>)</span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">    index = np.random.randint(<span class="number">0</span>, <span class="number">10000</span>)</span><br><span class="line">    plt.subplot(<span class="number">1</span>, <span class="number">4</span>, i + <span class="number">1</span>)</span><br><span class="line">    plt.axis(<span class="string">&quot;off&quot;</span>)</span><br><span class="line">    plt.imshow(X_test1[index])</span><br><span class="line">    plt.title(<span class="string">&quot;y=&quot;</span> + <span class="built_in">str</span>(class_names[<span class="built_in">int</span>(Y_test1[index])]) + <span class="string">&quot;\ny_pred=&quot;</span> + <span class="built_in">str</span>(class_names[prediction[index]]))</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<h6 id="实验结果-2">实验结果：</h6>
<p><img data-src="/images/VGGNet16_acc.png" /></p>
<p><img data-src="/images/VGGNet16_loss.png" /></p>
<p><img data-src="/images/VGGNet16_res.png" /></p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>深度学习</tag>
        <tag>CNN</tag>
      </tags>
  </entry>
  <entry>
    <title>Tensorflow2.x深度学习使用Sequential一般步骤</title>
    <url>/2022/04/06/Tensorflow2.x%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E4%B8%80%E8%88%AC%E6%AD%A5%E9%AA%A4/</url>
    <content><![CDATA[<h3
id="tensorflow2.x深度学习使用sequential一般步骤">Tensorflow2.x深度学习使用Sequential一般步骤</h3>
<hr />
<ol type="1">
<li>建立模型</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model = tf.keras.Sequential()</span><br><span class="line">model.add()</span><br></pre></td></tr></table></figure>
<ol start="2" type="1">
<li>查看摘要</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model.summary()</span><br></pre></td></tr></table></figure>
<ol start="3" type="1">
<li>配置训练方法</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model.<span class="built_in">compile</span>()</span><br></pre></td></tr></table></figure>
<ol start="4" type="1">
<li>训练模型</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model.fit()</span><br></pre></td></tr></table></figure>
<ol start="5" type="1">
<li>评估模型</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model.evaluate()</span><br></pre></td></tr></table></figure>
<ol start="6" type="1">
<li>使用模型</li>
</ol>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model.predict()</span><br></pre></td></tr></table></figure>
<hr />
<h4 id="实验代码">实验代码：</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 加载数据集</span></span><br><span class="line">mnist = tf.keras.datasets.mnist</span><br><span class="line">(train_x, train_y), (test_x, test_y) = mnist.load_data()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 数据预处理</span></span><br><span class="line">X_train = tf.cast(train_x/<span class="number">255.0</span>, tf.float32)</span><br><span class="line">X_test = tf.cast(test_x/<span class="number">255.0</span>, tf.float32)</span><br><span class="line">Y_train = tf.cast(train_y, tf.int16)</span><br><span class="line">Y_test = tf.cast(test_y, tf.int16)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 建立模型</span></span><br><span class="line">model = tf.keras.Sequential()</span><br><span class="line"><span class="comment"># 输入层，MNIST数据集每张图片大小为28*28，需要先将图片转换为一维张量</span></span><br><span class="line">model.add(tf.keras.layers.Flatten(input_shape=(<span class="number">28</span>, <span class="number">28</span>)))</span><br><span class="line"><span class="comment"># 一层隐藏层，为全连接层，节点个数为128</span></span><br><span class="line">model.add(tf.keras.layers.Dense(<span class="number">128</span>, activation=<span class="string">&quot;relu&quot;</span>))</span><br><span class="line"><span class="comment"># 输出层，MNIST共有10种结果，故节点数为10</span></span><br><span class="line">model.add(tf.keras.layers.Dense(<span class="number">10</span>, activation=<span class="string">&quot;softmax&quot;</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment"># 查看模型</span></span><br><span class="line">model.summary()</span><br><span class="line"></span><br><span class="line"><span class="comment"># 配置模型</span></span><br><span class="line">model.<span class="built_in">compile</span>(optimizer=<span class="string">&quot;adam&quot;</span>,</span><br><span class="line">              loss=<span class="string">&quot;sparse_categorical_crossentropy&quot;</span>,</span><br><span class="line">              metrics=[<span class="string">&quot;sparse_categorical_accuracy&quot;</span>])</span><br><span class="line"></span><br><span class="line"><span class="comment"># 训练模型</span></span><br><span class="line">model.fit(X_train, Y_train, batch_size=<span class="number">64</span>, epochs=<span class="number">10</span>, validation_split=<span class="number">0.25</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 评估模型</span></span><br><span class="line">model.evaluate(X_test, Y_test, verbose=<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 保存模型</span></span><br><span class="line">model.save(<span class="string">&quot;mnist_model&quot;</span>, save_format=<span class="string">&quot;tf&quot;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用模型</span></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">6</span>):</span><br><span class="line">    index = np.random.randint(<span class="number">0</span>, <span class="number">10000</span>)</span><br><span class="line">    <span class="comment"># 因为在输入层将数据转换为了一维数组，所以预测的时候也需要保证输入和输入层一致</span></span><br><span class="line">    demo = tf.reshape(X_test[index], (<span class="number">1</span>, <span class="number">28</span>, <span class="number">28</span>))</span><br><span class="line">    y_pred = np.argmax(model.predict(demo))</span><br><span class="line">    plt.subplot(<span class="number">1</span>, <span class="number">6</span>, i+<span class="number">1</span>)</span><br><span class="line">    plt.axis(<span class="string">&quot;off&quot;</span>)</span><br><span class="line">    plt.imshow(test_x[index], cmap=<span class="string">&quot;gray&quot;</span>)</span><br><span class="line">    plt.title(<span class="string">&quot;y=&quot;</span> + <span class="built_in">str</span>(test_y[index]) + <span class="string">&quot;\ny_pred=&quot;</span> + <span class="built_in">str</span>(y_pred))</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<hr />
<h4 id="实验结果">实验结果：</h4>
<p><img data-src="/images/mnist.png" /></p>
<hr />
<h4 id="实验总结">实验总结：</h4>
<p>其实对于一般的神经网络，构建的方法都很类似，它们主要的不同体现在对各种参数（比如优化器、激活函数、评价指标、损失函数）的选择以及隐藏层的数量、每一层的神经元个数上。我们需要注意的是虽然使用tf.keras.Sequential()构建神经网络只需要关注输入层和输出层的结构（隐藏层会自动根据上下层的结构生成对应的训练参数结构），但是必须保证使用模型预测时数据集的处理和模型一致（这也是本次实验中容易出错的地方）。</p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>深度学习</tag>
        <tag>Tensorflow</tag>
      </tags>
  </entry>
  <entry>
    <title>Tensorflow2.x实现多元逻辑回归</title>
    <url>/2022/04/05/Tensorflow2.x%E5%AE%9E%E7%8E%B0%E5%A4%9A%E5%85%83%E9%80%BB%E8%BE%91%E5%9B%9E%E5%BD%92/</url>
    <content><![CDATA[<h3
id="tensorflow2.x实现多元逻辑回归">Tensorflow2.x实现多元逻辑回归</h3>
<hr />
<p>鸢尾花数据集是经典的机器学习数据集，该数据集包含了鸢尾花的三种分类，即Setosa鸢尾花、Versicolour鸢尾花和Virginica鸢尾花，每一条数据用四个特征（萼长、萼宽、瓣长、瓣宽）来进行描述。</p>
<p>多元逻辑回归模型的一般过程：</p>
<ol type="1">
<li>设置参数</li>
<li>构建函数</li>
<li>根据损失函数反向求导</li>
<li>根据梯度分别更新参数</li>
</ol>
<hr />
<h4 id="实验代码">实验代码：</h4>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> tensorflow <span class="keyword">as</span> tf</span><br><span class="line"><span class="keyword">import</span> pandas <span class="keyword">as</span> pd</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> matplotlib.pyplot <span class="keyword">as</span> plt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment"># 下载数据集</span></span><br><span class="line">TRAIN_URL = <span class="string">&quot;https://download.tensorflow.org/data/iris_training.csv&quot;</span></span><br><span class="line">train_path = tf.keras.utils.get_file(TRAIN_URL.split(<span class="string">&#x27;/&#x27;</span>)[-<span class="number">1</span>], TRAIN_URL)</span><br><span class="line">TEST_URL = <span class="string">&quot;https://download.tensorflow.org/data/iris_test.csv&quot;</span></span><br><span class="line">test_path = tf.keras.utils.get_file(TEST_URL.split(<span class="string">&#x27;/&#x27;</span>)[-<span class="number">1</span>], TEST_URL)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 导入数据集</span></span><br><span class="line">df_iris_train = pd.read_csv(train_path, header=<span class="number">0</span>)</span><br><span class="line">df_iris_test = pd.read_csv(test_path, header=<span class="number">0</span>)</span><br><span class="line">iris_train = np.array(df_iris_train)</span><br><span class="line">iris_test = np.array(df_iris_test)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 对数据集数据进行随机打乱</span></span><br><span class="line">np.random.seed(<span class="number">520</span>)</span><br><span class="line">np.random.shuffle(iris_train)</span><br><span class="line">np.random.shuffle(iris_test)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 将数据的前4列特征作为训练数据，第5列标签提取出来</span></span><br><span class="line">x_train = iris_train[:, <span class="number">0</span>:<span class="number">4</span>]</span><br><span class="line">y_train = iris_train[:, <span class="number">4</span>]</span><br><span class="line">x_test = iris_test[:, <span class="number">0</span>:<span class="number">4</span>]</span><br><span class="line">y_test = iris_test[:, <span class="number">4</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 数据预处理</span></span><br><span class="line">x_train = x_train - np.mean(x_train, axis=<span class="number">0</span>)</span><br><span class="line">x_test = x_test - np.mean(x_test, axis=<span class="number">0</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># numpy读取数据默认float64，但在一般情况下，float32在满足精度要求下计算速度更快；对标签进行one-hot编码</span></span><br><span class="line">X_train = tf.cast(x_train, tf.float32)</span><br><span class="line">Y_train = tf.one_hot(tf.constant(y_train, dtype=tf.int32), <span class="number">3</span>)</span><br><span class="line">X_test = tf.cast(x_test, tf.float32)</span><br><span class="line">Y_test = tf.one_hot(tf.constant(y_test, dtype=tf.int32), <span class="number">3</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置超参数</span></span><br><span class="line">learn_rate = <span class="number">0.5</span></span><br><span class="line"><span class="built_in">iter</span> = <span class="number">100</span></span><br><span class="line">display_step = <span class="number">10</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 设置训练参数，构建模型</span></span><br><span class="line">W1 = tf.Variable(np.random.randn(<span class="number">4</span>, <span class="number">32</span>), dtype=tf.float32)</span><br><span class="line">B1 = tf.Variable(np.zeros([<span class="number">32</span>]), dtype=tf.float32)</span><br><span class="line">W2 = tf.Variable(np.random.randn(<span class="number">32</span>, <span class="number">16</span>), dtype=tf.float32)</span><br><span class="line">B2 = tf.Variable(np.zeros([<span class="number">16</span>]), dtype=tf.float32)</span><br><span class="line">W3 = tf.Variable(np.random.randn(<span class="number">16</span>, <span class="number">3</span>), dtype=tf.float32)</span><br><span class="line">B3 = tf.Variable(np.zeros([<span class="number">3</span>]), dtype=tf.float32)</span><br><span class="line"></span><br><span class="line">acc_train = []</span><br><span class="line">acc_test = []</span><br><span class="line">cce_train = []</span><br><span class="line">cce_test = []</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">0</span>, <span class="built_in">iter</span> + <span class="number">1</span>):</span><br><span class="line">    <span class="keyword">with</span> tf.GradientTape() <span class="keyword">as</span> tape:</span><br><span class="line">        Hidden1_train = tf.nn.relu(tf.matmul(X_train, W1) + B1)</span><br><span class="line">        Hidden2_train = tf.nn.relu(tf.matmul(Hidden1_train, W2) + B2)</span><br><span class="line">        PRED_train = tf.nn.softmax(tf.matmul(Hidden2_train, W3) + B3)</span><br><span class="line">        Loss_train = tf.reduce_mean(tf.keras.losses.categorical_crossentropy(y_true=Y_train, y_pred=PRED_train))</span><br><span class="line">    Hidden1_test = tf.nn.relu(tf.matmul(X_test, W1) + B1)</span><br><span class="line">    Hidden2_test = tf.nn.relu(tf.matmul(Hidden1_test, W2) + B2)</span><br><span class="line">    PRED_test = tf.nn.softmax(tf.matmul(Hidden2_test, W3) + B3)</span><br><span class="line">    Loss_test = tf.reduce_mean(tf.keras.losses.categorical_crossentropy(y_true=Y_test, y_pred=PRED_test))</span><br><span class="line"></span><br><span class="line">    accuracy_train = tf.reduce_mean(tf.cast(tf.equal(tf.argmax(PRED_train.numpy(), axis=<span class="number">1</span>), y_train), tf.float32))</span><br><span class="line">    accuracy_test = tf.reduce_mean(tf.cast(tf.equal(tf.argmax(PRED_test.numpy(), axis=<span class="number">1</span>), y_test), tf.float32))</span><br><span class="line">    acc_train.append(accuracy_train)</span><br><span class="line">    acc_test.append(accuracy_test)</span><br><span class="line">    cce_train.append(Loss_train)</span><br><span class="line">    cce_test.append(Loss_test)</span><br><span class="line"></span><br><span class="line">    grads = tape.gradient(Loss_train, [W1, B1, W2, B2, W3, B3])</span><br><span class="line">    W1.assign_sub(learn_rate * grads[<span class="number">0</span>])</span><br><span class="line">    B1.assign_sub(learn_rate * grads[<span class="number">1</span>])</span><br><span class="line">    W2.assign_sub(learn_rate * grads[<span class="number">2</span>])</span><br><span class="line">    B2.assign_sub(learn_rate * grads[<span class="number">3</span>])</span><br><span class="line">    W3.assign_sub(learn_rate * grads[<span class="number">4</span>])</span><br><span class="line">    B3.assign_sub(learn_rate * grads[<span class="number">5</span>])</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> i % display_step == <span class="number">0</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;i: %i, TrainAcc: %f, TrainLoss: %f, TestAcc: %f, TestLoss: %f&quot;</span> % (i, accuracy_train, Loss_train, accuracy_test, Loss_test))</span><br><span class="line"></span><br><span class="line">plt.figure(figsize=(<span class="number">20</span>, <span class="number">6</span>))</span><br><span class="line"></span><br><span class="line">plt.subplot(<span class="number">121</span>)</span><br><span class="line">plt.plot(cce_train, color=<span class="string">&quot;blue&quot;</span>, label=<span class="string">&quot;train&quot;</span>)</span><br><span class="line">plt.plot(cce_test, color=<span class="string">&quot;red&quot;</span>, label=<span class="string">&quot;test&quot;</span>)</span><br><span class="line">plt.xlabel(<span class="string">&quot;Iteration&quot;</span>)</span><br><span class="line">plt.ylabel(<span class="string">&quot;Loss&quot;</span>)</span><br><span class="line">plt.legend()</span><br><span class="line"></span><br><span class="line">plt.subplot(<span class="number">122</span>)</span><br><span class="line">plt.plot(acc_train, color=<span class="string">&quot;blue&quot;</span>, label=<span class="string">&quot;train&quot;</span>)</span><br><span class="line">plt.plot(acc_test, color=<span class="string">&quot;red&quot;</span>, label=<span class="string">&quot;test&quot;</span>)</span><br><span class="line">plt.xlabel(<span class="string">&quot;Iteration&quot;</span>)</span><br><span class="line">plt.ylabel(<span class="string">&quot;Accuracy&quot;</span>)</span><br><span class="line">plt.legend()</span><br><span class="line"></span><br><span class="line">plt.tight_layout()</span><br><span class="line">plt.show()</span><br></pre></td></tr></table></figure>
<hr />
<h4 id="实验结果">实验结果：</h4>
<p><img data-src="/images/iris.png" /></p>
<hr />
<h4 id="实验总结">实验总结：</h4>
<p>多元逻辑回归就是找到一条曲线（曲面）去划分多分类，整体相当于对于f=Wx
+
B求得损失函数最小的参数，其中x和b是维度相同的一维向量，其大小代表了数据的维度。实验的主要工作在于应用tensorflow2.5的api构建函数以及对参数的求导（偏导），我们使用tf.matmul()构建函数，使用tf.GradientTape()进行自动求导，使用tensorflow的一大优势就在于我们不用关心损失函数复杂的链式求导过程，而可以将重心放在损失函数的设计以及其他参数的选择上。</p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>深度学习</tag>
        <tag>Tensorflow</tag>
        <tag>多元逻辑回归</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2021/12/01/hello-world/</url>
    <content><![CDATA[<p>Welcome to <span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvLw==">Hexo<i class="fa fa-external-link-alt"></i></span>! This is your very
first post. Check <span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL2RvY3Mv">documentation<i class="fa fa-external-link-alt"></i></span> for
more info. If you get any problems when using Hexo, you can find the
answer in <span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL2RvY3MvdHJvdWJsZXNob290aW5nLmh0bWw=">troubleshooting<i class="fa fa-external-link-alt"></i></span> or
you can ask me on <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2hleG9qcy9oZXhvL2lzc3Vlcw==">GitHub<i class="fa fa-external-link-alt"></i></span>.</p>
<h2 id="quick-start">Quick Start</h2>
<h3 id="create-a-new-post">Create a new post</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>
<p>More info: <span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL2RvY3Mvd3JpdGluZy5odG1s">Writing<i class="fa fa-external-link-alt"></i></span></p>
<h3 id="run-server">Run server</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>
<p>More info: <span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL2RvY3Mvc2VydmVyLmh0bWw=">Server<i class="fa fa-external-link-alt"></i></span></p>
<h3 id="generate-static-files">Generate static files</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>
<p>More info: <span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL2RvY3MvZ2VuZXJhdGluZy5odG1s">Generating<i class="fa fa-external-link-alt"></i></span></p>
<h3 id="deploy-to-remote-sites">Deploy to remote sites</h3>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>
<p>More info: <span class="exturl" data-url="aHR0cHM6Ly9oZXhvLmlvL2RvY3Mvb25lLWNvbW1hbmQtZGVwbG95bWVudC5odG1s">Deployment<i class="fa fa-external-link-alt"></i></span></p>
]]></content>
  </entry>
  <entry>
    <title>语录</title>
    <url>/2021/12/07/%E8%AF%AD%E5%BD%95/</url>
    <content><![CDATA[<h2 id="语录">语录</h2>
<p><img data-src="/images/sentence_top_img.jpg" /></p>
<p>​
也许吧，因为人要接受事与愿违啊。我们太有限了，我们只能做我们觉得是对的事情，然后接受，它的事与愿违。因为人生所有东西，它都是有负斜率的。在跑的时候，可能慢慢的负斜率，使得偏离了最初的目标，这是人的有限性。所以这就是为什么不要对自己所投入的事情、所看中的事情，抱有不加边际的幻想和热情。</p>
<p align="right">
—— 罗翔
</p>
<p>  苏格拉底说过：“对于你懂的东西，你才会真正地认同。对于你不懂的东西，你不可能真正地认同。”</p>
<p align="right">
—— 罗翔
</p>
<p>  人最大的痛苦就是在于无法跨越知道和做到的那个鸿沟。笛卡尔说：“我思故我在”，这个很对，但是，“我思故我在”
会经常让我们沉浸于一种幻想之中，就是我们只要思考到了，我们就能做到。但其实不是这样。</p>
<p align="right">
—— 罗翔
</p>
<p>  人片刻的感动，不可能持久的感动。很多人所谓的被你影响，只是片刻的感动了，最终还是投入到自己以前的生活。片刻的感动，在很多时候是一种自我欺骗。你在欺骗自己，你也在欺骗别人，你也让别人陷入到一种自我欺骗。</p>
<p align="right">
—— 罗翔
</p>
<p>  我们画不出一个完美的圆，但完美的圆是存在的。虽不能至，心向往之。</p>
<p align="right">
—— 罗翔
</p>
<p>  人都是会犯错误的，所以就是永远要接受我们所有的看法都是在偏见之中。我们的一生都是像苏格拉底所说的，走出那个偏见的洞穴。</p>
<p align="right">
—— 罗翔
</p>
<p>  人这一生自己能决定的也许只有 5%，有 95%
是你决定不了的，你的努力也许只是那 5% 的一个支点，撬动了那 95%
的命运。但是你不要把所有的功劳归给你自己，你飞得越高，你要注意有根线是拉着你的，像气球一样，如果你说我太厉害了，我没有必要受这个线的约束，最后你会爆炸的。因此你要感恩你生命中所拥有的一切。</p>
<p align="right">
—— 罗翔
</p>
<p>  奥古斯丁在《忏悔录》中说过这样一段话：“当你问我时间是什么，如果你不问我，我感觉自己还知道，你一旦问我，我就茫然无知了。”
昨天的已经成为过去，明天的还没有到来，所以今天是一个礼物，大家把今天当作一个礼物，去珍惜享受你现在拥有的平凡的幸福。因为这种幸福现在你觉得平凡，但到你失去那一天，你会觉得它是如此的珍贵。</p>
<p align="right">
—— 罗翔
</p>
<p>  世界即不黑也不白，而是一道精致的灰。（The world is not black or
white, but a delicious shade of grey.）</p>
<p align="right">
—— 英雄联盟卡密尔台词
</p>
<p>  人在少年的时候，大多会经历一场情不知所起一往而深的感情，或者是因为他成绩好，或者是他擅长打篮球，或者是言行有趣，或者是长得好看。总之就是某个不经意的刹那，怦然心动。但大多数时候，它来得突然，却也无疾而终，在某年某月，等你再回首的时候，想起自己曾经的一往情深，不免哑然失笑。那些曾经我们看来很美好的事物，也会随着时间的流逝，而变得无关紧要。对于很多人来说终其一生会遇到很多人，但可能要倾尽所有的运气，才会遇见到特别的那一个。</p>
<p align="right">
——b 站 up 主阿斗归来了讲解电影《怦然心动》
</p>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>读书</tag>
        <tag>语录</tag>
      </tags>
  </entry>
  <entry>
    <title>Java核心技术·卷Ⅰ读书笔记</title>
    <url>/2021/12/07/Java%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="java核心技术卷ⅰ读书笔记">Java核心技术·卷Ⅰ读书笔记</h2>
<p><img data-src="/images/java_top_img.png" /></p>
<h4 id="数据类型">数据类型</h4>
<p>​
Java中一共8种基本类型：int、short、long、byte、float、double、char、boolean。</p>
<p>​
Java中没有任何无符号（unsigned）形式的int、short、long或者byte类型。</p>
<p>​
不同于其他语言，Java整型的范围与运行Java代码的机器无关，而例如C/C++中，int在16位机器上占2字节，在32位机器上占4字节，在64位机器上占8字节。</p>
<p>​
浮点数值不适用于无法接受舍入误差的金融计算中。舍入误差产生的原因：根据IEEE
754标准，单精度float类型使用32比特存储，其中1位表示符号，8位表示指数，23位表示尾数；双精度double类型使用64比特存储，1位符号位，11位指数位，52位尾数位。</p>
<p>​
Float精度为6-7位，Double精度为15位，所以说Int类型的数据转向Float会损失精度，例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> intData1=<span class="number">123</span>;</span><br><span class="line"><span class="type">int</span> intData2=<span class="number">123456789</span>;</span><br><span class="line">System.out.println((<span class="type">float</span>)intData1);</span><br><span class="line">System.out.println((<span class="type">float</span>)intData2);</span><br></pre></td></tr></table></figure>
<p>结果为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="number">123.0</span></span><br><span class="line"><span class="number">1.23456792E8</span>  <span class="comment">//此处已经损失精度</span></span><br></pre></td></tr></table></figure>
<p>​ 整型值和布尔值之间不能相互转换。</p>
<p>​
Java中父类强制转换成子类的原则：父类型的引用指向的是哪个子类的实例，就能转换成哪个子类的引用。父类转子类的前提是：此父类对象为子类对象的引用。</p>
<p>​ 从Java 7开始，加上前缀 0b 就可以写成二进制数。例如
0b1010就是十进制数10，另外，同样是从Java
7开始，还可以在数字字面量加下划线，如用1_000_000(或
0b1111_0100_0010_0000)表示一百万。这些下划线只是为了让人更容易阅读，Java编译器会去除这些下划线。</p>
<p><img data-src="/images/java_type.png" /></p>
<h4 id="变量">变量</h4>
<p>​ 声明一个变量后，必须用赋值语句对变量进行显式初始化。</p>
<p>​ 建议变量的声明尽可能地靠近变量第一次使用的地方。</p>
<p>​ 在Java中，利用关键字final指示常量，常量名习惯上使用全大写。</p>
<p>​
final修饰对象引用相当于一个常量指针，在内存中的地址不能改变，但是可以修改引用指向内存地址的具体内容。</p>
<p>​ 如果希望一个常量在一个类的多个方法中都可以使用，可以将常量用 static
final 修饰；类常量的定义位于main方法外。</p>
<p>​ const是Java的保留关键字，但是还没有使用。</p>
<p>​
Java中的所有参数都通过值传递。当参数是原始数据类型时，实际参数的值将复制到参数中，对方法主体中的参数值进行的任何更改只会更改形式参数的副本，而不会更改实际参数的值；而当参数是引用类型时，存储在实际参数中的引用被复制到形式参数，此时实际参数和形式参数都指向内存中的相同对象，在方法主体中对引用变量做出的任何操作将会改变实参指向的对象。</p>
<h4 id="运算符">运算符</h4>
<p>​ 参与 /
（除法）运算的两个操作数都是整数时，表示整数除法，否则表示浮点数除法。</p>
<p>​ 只有整数才有 % （求余）运算。</p>
<p>​
低精度向高精度转换不存在精度丢失，称为隐式类型转换；反之，高精度向低精度转换可能存在精度丢失，称为显示类型转换，或者强制类型转换。</p>
<p>​ 逻辑运算符 &amp;&amp; 和 || 按照"短路"方式求值，且 &amp;&amp;
的优先级高于 || ；但是 &amp; 和 |
不遵循"短路"的原则，也可以达到逻辑与和或的目的。</p>
<p>​
逻辑位移是移动数字的所有物理比特位；算数位移是对数字除了符号位以外的比特位进行位移，符号位保持不变。</p>
<p>​ 在Java中，使用 &gt;&gt;&gt; 表示逻辑位移，使用 &gt;&gt;
表示算数位移； &gt;&gt; (算术右移)将用符号位填充高位， &gt;&gt;&gt;
会用0填充最高位，不存在 &lt;&lt;&lt; 的运算。</p>
<p><img data-src="/images/java_operator.png" /></p>
<h4 id="字符串">字符串</h4>
<p>​ 静态方法 join() 可以将字符串以一个分界符连接起来。</p>
<p>​
Java中String类型是不可变字符串，字符串不可变的优点在于编译器可以让字符串共享。</p>
<p>​ ==
运算符用于判断字符串对象时判断的是对象的引用是否相同，即引用是否指向内存中同一个地址，如果需要判断字符串的内容是否一致，则需要调用方法
equals() 。</p>
<p>​
如果需要以频繁地拼接字符串作为构建字符串的方式，则使用StringBuilder类（非线程安全，效率高）或者StringBuffer类（线程安全）更好。</p>
<h4 id="数组">数组</h4>
<p>​ 数组长度不要求是常量。</p>
<p>​
创建一个数字数组时，所有元素初始化为0；创建一个对象数组时，所有元素初始化为
null ；创建一个 boolean 数组时，所有元素初始化为 false 。</p>
<p>​ Arrays 类的 toString() 方法可以快递打印一个数组。</p>
<p>​
数组的拷贝默认是浅层拷贝，即复制的是数组对象的引用，前后两者指向同一片内存；若要深层拷贝，则需要调
Arrays 类的 copyOf() 方法。</p>
<h4 id="对象与类">对象与类</h4>
<p>​ 对象的三个主要特性：行为；状态；标识。</p>
<p>​ 类之间的关系：依赖；继承；聚合。</p>
<p>​
依赖是一种“uses-a”的关系，如果一个类的方法操作另一个类的对象，则说前者依赖于后者。应尽可能地减少依赖关系，如果类A不知道类B的存在，那么任何对类B的修改都不会引起类A的变化，即类之间的耦合度越小越好。</p>
<p>​ 聚合是一种“has-a”的关系，即一个类的对象中包含另一个类的对象。</p>
<p>​ 继承是一种“is-a”的关系，是一种用于表示特殊与一般的关系。</p>
<p>​ 一个对象变量并没有实际包含一个对象，仅仅只是一个对象的引用。</p>
<p>​ 局部变量不会自动地初始化为 null ，必须通过调用 new
对其进行初始化或者将他们设置为 null 。</p>
<p>​
封装的优点：保证该类的内部修改不会影响到其他的代码；尽量提供独立的域访问器和域更改器，保证实例域值不受到外界的破坏，即不提倡直接以成员变量访问实例域值的方式。</p>
<p>​
如果构造器没有显式地给域赋初始值，那么会自动地被赋初始值，这也是域与局部变量的主要不同点，但是一般还是建议对域都进行初始化。</p>
<p>​
如果类中提供了至少一个构造器，但是没有提供无参数的构造器，则在构造对象时如果没有提供参数就会被视为不合法。</p>
<p>​ 如果构造器的第一个语句形如 this(...)
，这个构造器将调用同一个类的另一个构造器。采用这种方式使用 this
关键字非常有用，这样对公共的构造器代码部分只编写一次即可。</p>
<p>​
在一个类的声明中，可以包含多个代码块。只要构造类的对象，这些块就会被执行。</p>
<p>​ 一个类可以使用所属包中的所有类和其他包中的公有类。</p>
<p>​
类设计技巧：一定要保证数据私有；一定要对数据初始化；不要在类中使用过多的基本类型；不是所有的域都需要独立的域访问器和域更改器（比如创建时间等初始化后无需更改的数据）；将职责过多的类进行分解；类名和方法名要能够体现他们的职责；优先使用不可变的类（保证多线程安全）。</p>
<h4 id="继承">继承</h4>
<p>​
在Java中，所有继承都是公有继承（不同于C++中存在私有继承和保护继承）。</p>
<p>​
在子类中可以增加域、增加方法、覆盖超类的方法，但是不能删除继承的任何域和方法。</p>
<p>​ 子类想要访问超类的域一般要借助超类的域访问器，使用关键字 super
。</p>
<p>​ 我们可以通过super 实现对超类构造器的调用，使用 super
调用构造器的语句必须是子类构造器的第一条语句。</p>
<p>​
如果子类的构造器没有显式地调用超类的构造器，则将自动地调用超类默认（没有参数）的构造器。如果超类没有不带参数的构造器，并且在子类的构造器中又没有显式地调用超类的其他构造器，则Java
编译器将报告错误。</p>
<p>​ 一个对象变量（例如，变量e
）可以指示多种实际类型的现象被称为多态（polymorphism）；在运行时能够自动地选择调用哪个方法的现象称为动态绑定（dynamic
binding）。</p>
<p>​ Java不支持多继承。</p>
<p>​
程序中出现超类对象的任何地方都可以用子类对象置换，例如，可以将一个子类的对象赋值给超类变量，但是反之则是不成立的。</p>
<p>​
包含一个或者多个抽象方法的类必须被声明为抽象类；抽象类中依然可以包含具体数据和具体方法。</p>
<p>​
抽象类不能被实例化，但是可以创建抽象类的对象变量，它只能指向具体子类的对象。</p>
<p>​ Java规定 equals()
方法具有如下特性：自反性；对称性；传递性；一致性；对于任意非空引用x，<code>x.equals(null)</code>
返回false。</p>
<p>​
ArrayList是一个采用类型参数的泛型类，可以实现动态数组；但是他不能直接使用[]访问数组元素，必须借助
set() 和 get()
方法。可以将一个类型化的数组列表传递给一个原始无参数ArrayList而不需要任何类型转换；也可以将一个原始无参数ArrayList赋值给一个类型化ArrayList（这样将会得到一个警告）。</p>
<p>​
包装器：Integer、Long、Float、Double、Short、Byte、Character、Void、Boolean，对象包装器是不可变的，一旦构造了包装器，就不允许更改包装在其中的值。</p>
<p>​
继承的设计技巧：将公共操作和域放在超类；使用继承实现“is-a”的关系；除非所有继承的方法都有意义，否则不要使用继承；在覆盖方法时，不要改变预期的行为；使用多态而非类型信息（即不要通过判断对象的类型来确定执行同一概念的不同行为，可以将该行为放置在超类或者接口中，通过多态性提供的动态分派机制执行相应的行为）；不要过多地使用反射。</p>
<h4 id="接口内部类">接口、内部类</h4>
<p>​
接口决不能含有实例域，但却可以包含常量，可以实现一些简单的方法。</p>
<p>​ Java中的类可以实现任意数量的接口。</p>
<p>​ 实现接口必须实现其中所有的方法。</p>
<p>​
在接口中，所有的方法默认都是public的，但是在实现接口中，必须声明方法为public；接口中的域默认是
public static final 的。</p>
<p>​ 可以为接口方法提供一个默认实现，必须用 default
修饰符标记；且默认方法可以调用任何其他方法。</p>
<p>​
为接口增加一个非默认方法不能保证“源代码兼容”，在之前的接口中添加非默认方法可能导致之前实现接口的类编译出错（因为没有实现新加入的方法）。</p>
<p>​
解决默认方法冲突：超类优先；接口冲突。如果一个类继承自一个超类并且实现了一个接口，且超类提供了一个和接口冲突的方法，此时接口的方法（无论是否存在默认方法都将失效，满足类优先原则）；如果一个类实现了两个接口，且两个接口中含有冲突的方法，且一个接口存在默认方法，必须程序员自己实现该方法来覆盖接口的方法（并不是直接选取其中的一个默认实现），如果两个接口冲突的方法都不存在默认方法，则该类本身就是抽象的，不存在方法冲突的问题。</p>
<p>​ lambda表达式形式：（参数） -&gt;
表达式，表达式可以是代码块并用return语句返回，即使没有参数，也不能省略小括号；如果参数类型可以被推导出来，则可以省略参数类型；如果参数只有一个且类型可以被推导出来，则可以省略小括号和类型。</p>
<p>​
无需指定lambda表达式的返回值类型，它总是可以根据上下文推导出来的。</p>
<p>​
如果一个lambda表达式只在部分分支返回值，而在另一些分支不返回值，这样的表达式是不合法的。</p>
<p>​
对于只有一个抽象方法的接口，需要这种接口的对象时，就可以提供一个lambda表达式，这种接口成为函数式接口。</p>
<p>​
lambda表达式有三个部分：一个代码块；参数；自由变量的值，指的是非参数而且不在代码中定义的量，但是在lambda表达式中只能引用值不会改变的变量。</p>
<p>​
内部类既可以访问自身的数据域，也可以访问创建它的外围类对象的数据域。</p>
<p>​ 内部类中声明的所有静态域都必须是 final 的；内部类中不允许有 static
方法。</p>
<h4 id="异常断言和日志">异常、断言和日志</h4>
<p>​ 在Java中，异常对象都是派生于 Throwable
类的一个实例。需要注意的是，虽然所有的异常都是由Throwable继承而来，但在下一层立即分解为两个分支：Error和Exception。</p>
<p>​ Error类层次结构描述了Java
运行时系统的内部错误和资源耗尽错误。应用程序不应该抛出这种类型的对象。如果出现了这样的内部错误，除了通告给用户，并尽力使程序安全地终止之外，再也无能为力了。这种情况很少出现。</p>
<p>​
由程序错误导致的异常属于RuntimeException;而程序本身没有问题，但由于像I/O错误这类问题导致的异常属于其他异常。</p>
<p>​ 派生于RuntimeException的异常包含下面几种情况： ​ •错误的类型转换。
​ •数组访问越界i。 ​ •访问null指针。 ​ 不是派生于RuntimeException
的异常包括： ​ •试图在文件尾部后面读取数据。 ​
•试图打开一个不存在的文件。 ​ •试图根据给定的字符串查找Class
对象，而这个字符串表示的类并不存在。</p>
<p>​ throws抛出的是异常类，throw抛出的是具体异常类实例化的对象。</p>
<p>​
当多个异常捕获处理的逻辑一样时，可以用一个catch语句块进行捕获。但是只有当捕获的异常类型彼此之间不存在子类关系时才可以用一个catch语句块捕获多个异常，并且捕获多个异常时，异常变量隐含为final变量。</p>
<p>​ 在catch子句中可以抛出一个异常，这样做的目的是改变异常的类型。</p>
<p>​ 当finally子句包含return
语句时，将会出现一种意想不到的结果，假设利用return语句从try语句块中退出。在方法返回前，finally
子句的内容将被执行。如果finally 子句中也有一个return
语句，这个返回值将会覆盖原始的返回值。具体来说，就是先执行try语句块中的return语句，但并不返回，继续执行finally语句块中的语句，执行完后再返回。</p>
<h4 id="泛型程序设计">泛型程序设计</h4>
<p>​ 在Java SE 7 及以后的版本中，
构造函数中可以省略泛型类型，比如：<code>ArrayList&lt;String&gt; files = new ArrayList&lt;&gt;();</code></p>
<p>​ 类型参数的魅力在于：使得程序具有更好的可读性和安全性。</p>
<p>​ 泛型类可看作普通类的工厂。</p>
<p>​
泛型方法可以定义在普通类中，也可以定义在泛型类中。当调用一个泛型方法时，在方法名前的尖括号中放人具体的类型。</p>
<p>​ 一个类型变量或通配符可以有多个限定，
例如：<code>T extends Comparable &amp; Serializable</code> ，限定类型用“
&amp;” 分隔，而逗号用来分隔类型变量。</p>
<h4 id="集合">集合</h4>
<p><img data-src="/images/java_collection.png" /></p>
<p>​
当在程序中使用队列时，一旦构建了集合就不需要知道究竟使用了哪种实现。因此，只有在构建集合对象时，使用具体的类才有意义。可以使用接口类型存放集合的引用，比如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Queue&lt;Customer&gt; expressLane = <span class="keyword">new</span> <span class="title class_">CircularArrayQueue</span>&lt;&gt;(<span class="number">100</span>) :</span><br><span class="line">expressLane.add(<span class="keyword">new</span> <span class="title class_">Customer</span>(<span class="string">&quot;Harry&quot;</span>));</span><br></pre></td></tr></table></figure>
<p>利用这种方式，一旦改变了想法，可以轻松地使用另外一种不同的实现。只需要对程序的一个地方做出修改，即调用构造器的地方。如果觉得LinkedListQueue是个更好的选择，就将代码修改为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Queue&lt;Customer&gt; expressLane = <span class="keyword">new</span> <span class="title class_">LinkedListQueue</span>&lt;&gt;() ;</span><br><span class="line">expressLane.add(<span class="keyword">new</span> <span class="title class_">Customer</span>(<span class="string">&quot;Harry&quot;</span>));</span><br></pre></td></tr></table></figure>
<p>​
访问集合元素时，需要先使用hasNext方法判断集合中是否还有元素，如果有的话再使用next方法访问。</p>
<p>​
利用迭代器删除集合元素的时候必须先访问再删除，即remove方法的调用依赖于next方法，类似的，不能连续调用两次remove方法来删除连续的两个集合元素。</p>
<p>​ Iterator和ListIterator之间的主要区别：</p>
<p>​ 1、遍历</p>
<p>​
使用Iterator，可以遍历所有集合，如Map，List，Set；但只能在向前方向上遍历集合中的元素。</p>
<p>​
使用ListIterator，只能遍历List实现的对象，但可以向前和向后遍历集合中的元素。</p>
<p>​ 2、添加元素</p>
<p>​ Iterator无法向集合中添加元素；而ListIteror可以向集合添加元素。</p>
<p>​ 3、修改元素</p>
<p>​
Iterator无法修改集合中的元素；而ListIterator可以使用set()方法修改集合中的元素。set()方法用一个新元素取代调用next()或previous()方法返回的上一个元素。</p>
<p>​ 4、索引</p>
<p>​
Iterator无法获取集合中元素的索引；而使用ListIterator，可以获取集合中元素的索引。</p>
<p>​
ArrayList和Vector的主要区别在于Vector类的所有方法都是同步的，而ArrayList方法不是同步的。</p>
<p>​ 对于散列表，在Java中用链表数组实现，每个列表被称作桶。在JavaSE 8
中，桶满时会从链表变为平衡二叉树。如果选择的散列函数不当，会产生很多冲突，或者如果有恶意代码试图在散列表中填充多个有相同散列码的值，这样就能提高性能。</p>
<p>​
如果大致知道最终会有多少个元素要插人到散列表中，就可以设置桶数。通常，将桶数设置为预计元素个数的75%
~
150%。有些研究人员认为：尽管还没有确凿的证据，但最好将桶数设置为一个素数，以防键的集聚。</p>
<p>​ 散列表可以用于实现几个重要的数据结构。其中最简单的是set
类型，set是没有重复元素的元素集合。set的add方法首先在集合中查找要添加的对象，如果不存在，就将这个对象添加进去。Java集合类库提供了一个HashSet类，它实现了基于散列表的集合，可以用add方法添加元素。contains方法已经被重新定义，用来快速地查看某个元素是否已经出现在集中，它只在某个桶中査找元素，而不必查看集合中的所有元素。同时，如果元素的散列码发生了改变（一般来说就是值改变），元素在数据结构中的位置也会发生变化。</p>
<p>​
TreeSet类（当前使用红黑树实现）与散列集十分类似，不过，它比散列集有所改进。树集是一个有序集合(
sorted collection )
，可以以任意顺序将元素插入到集合中。在对集合进行遍历时，每个值将自动地按照排序后的顺序呈现。</p>
<p>​
要使用树集，必须能够比较元素。这些元素必须实现Comparable接口，或者构造集时必须提供一个Comparator。为此，并不是树集就优于散列表，虽然表面上看树集比散列表多了有序的性质，但是在一些情况下，元素之间的比较不是那么容易，计算散列值一般都是比较容易的，此时在不要求元素有序的情况下就可以选择使用散列表。</p>
<p>​
队列可以让人们有效地在尾部添加一个元素，在头部删除一个元素。有两个端头的队列，即双端队列，可以让人们有效地在头部和尾部同时添加或删除元素，不支持在队列中间添加元素。在Java
SE
6中引人了Deque接口，并由ArrayDeque和LinkedList类实现。这两个类都提供了双端队列，而且在必要时可以增加队列的长度。</p>
<p>​
通常，我们知道某些键的信息，并想要查找与之对应的元素。映射（map）就是为此设计的。映射用来存放键/值对，如果提供了键，就能够查找到值。Java类库为映射提供了两个通用的实现：HashMap和TreeMap，这两个类都实现了Map接口。</p>
<p>​
散列映射对键进行散列，树映射用键的整体顺序对元素进行排序，并将其组织成搜索树。</p>
]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
        <tag>读书笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>深度学习之激活函数</title>
    <url>/2022/04/01/%E6%B7%B1%E5%BA%A6%E5%AD%A6%E4%B9%A0%E4%B9%8B%E6%BF%80%E6%B4%BB%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h2 id="深度学习之激活函数">深度学习之激活函数</h2>
<p>​
最近在系统地学习一些深度学习的知识，发现其中的激活函数很有意思，激活函数的选择也往往可以作为深度学习中调优的一种手段，针对不同的数据、不同的问题采取合适的激活函数，往往可以取得比较好的效果。结合网上的一些文章以及自己的理解，在此记录一下以便之后复习。
​
首先要明确的是激活函数的作用，激活函数在神经网络中将神经元上经过权重计算完毕的输入以某种方式（根据具体选择的激活函数）进行映射，从而输出作为该神经元对于下一层网络的输入，那么明明已经对输入根据参数进行了权重计算为什么仍然需要激活函数呢？激活函数的作用在于增加网络的非线性，我们知道，一般的权重计算往往是Wx
+
b的形式，即神经元对于输入进行线性组合，如果不使用激活函数，那么无论神经网络有多少层，最终的结果仍然是线性的，即该计算可以用一层网络替代，这种神经网络一般不能解决复杂一点的问题。而使用激活函数后，每一层网络间就不仅仅是线性的叠加，我们通过使用非线性激活函数的方式来使得神经网络具有了解决非线性问题的能力。
​ 以下是常用的几种激活函数。</p>
<h4 id="sigmoid函数">1.Sigmoid函数</h4>
<p>公式： <span class="math display">\[ f(x)=\frac{1}{1+e^{-x}}
\]</span> 图像：</p>
<p><img data-src="/images/sigmoid.png" /></p>
<p>​
Sigmoid是早期较为常用的激活函数，我们可以看出，Sigmoid函数的值域为(0,
1)，这相当于对数据进行了归一化处理，又因为概率的值域为[0,
1]，故Sigmoid很适合一些将概率作为输出的模型。同时，Sigmoid的图像比较平滑，这避免了输出存在较大的跳跃。</p>
<p>​
但是，Sigmoid的平均值不是0（一般来说输出的均值为0的模型效果较好一些），同时，因为函数涉及到指数计算，而参数的更新又依赖于梯度下降，所以计算的时间一般较长。</p>
<h4
id="tanhhyperbolic-tangent-function双曲正切激活函数">2.Tanh（Hyperbolic
tangent function，双曲正切激活函数）</h4>
<p>公式： <span class="math display">\[
f(x)=tanh(x)=\frac{e^x-e^{-x}}{e^x+e^{-x}}=\frac{2}{1+e^{-2x}}-1
\]</span> 图像：</p>
<p><img data-src="/images/tanh.png" /></p>
<p>​
同Sigmoid函数类似，Tanh函数也是一条平滑的S型曲线，但是Tanh函数的平均值为0，同时，对于负输入，Tanh将其映射为负，对于正输入，Tanh将其映射为正</p>
<h4 id="relulinear-rectification-function线性整流函数">3.ReLU（Linear
rectification function，线性整流函数）</h4>
<p>公式： <span class="math display">\[ f(x)=\begin{cases} x，x\geq0\\
0， x&lt;0\end{cases}=max(0,x) \]</span> 图像：</p>
<p><img data-src="/images/relu.png" /></p>
<p>​
ReLU函数是深度学习中较为流行的一种激活函数，ReLU可以实现单侧抑制（即把一部分神经元置0），能够稀疏模型，Sigmoid
和Tanh激活函数会导致一个非稀疏的神经网络，而ReLU大约 50%
的神经元会处于激活状态，具有很好的稀疏性。同时，ReLU函数右侧线性部分梯度始终为1，具有宽兴奋边界的特性（即兴奋程度可以非常高），不会发生神经网络的梯度消失问题，能够加速梯度下降的收敛速度。而Tanh和Sigmoid在离0点近的时候梯度大，在远离0点的时候梯度小，容易出现梯度消失。ReLU的计算也较为简单。</p>
<p>​
ReLU的缺点在于ReLU函数的值域在0到正无穷，相当于给后一层的神经网络引入偏置偏移，会影响梯度下降的效率。另外，在训练时，如果参数在一次不恰当的更新后，某个ReLU神经元输出为0，那么这个神经元自身参数的梯度永远都会是0，在以后的训练过程中永远不能被激活。</p>
<h4 id="leaky-relu-带泄露线性整流函数">4.Leaky ReLU
（带泄露线性整流函数）</h4>
<p>公式： <span class="math display">\[ f(x)=\begin{cases} x，x\geq0\\
\gamma x， x&lt;0\end{cases}=max(0,x)+\gamma min(0,x) \]</span>
图像：</p>
<p><img data-src="/images/lrelu.png" /></p>
<p>​ 这里，r取0.01（r通常是0到1之间的数，通常取小于1的很小常数）。</p>
<p>​ Leaky
ReLU函数在输入x&lt;0时，能保持一个很小的梯度r。这样当神经元非激活时也能有一个非零的梯度可以更新参数，避免永远不能被激活，以便给网络继续学习的机会。特殊的，当不确定r，将r作为一个可学习的参数时，这种激活函数为PReLU。</p>
<p>​ 总结性的：当r=0时，为ReLU；当r=常数时，为Leaky
ReLU；当r=训练的参数时，为PReLU。理论上，后两者具有ReLU的所有优点并且避免了Dead
ReLU问题，但是并没有实验能够证明后两者一定比ReLU更为优秀。一般在实验当中，我们可以首先试试ReLU的效果，当结果不大理想时可以试一试Leaky
ReLU，而因为PReLU增加了需要学习的参数，建议只在训练数据集较大的情况下使用。</p>
<h4 id="eluexponential-linear-unit指数线性单元">5.ELU（Exponential
Linear Unit，指数线性单元）</h4>
<p>公式： <span class="math display">\[ f(x)=\begin{cases} x，x\geq0\\
\alpha (e^x-1)， x&lt;0\end{cases} \]</span> 图像：</p>
<p><img data-src="/images/elu.png" /></p>
<p>​ 这里a取1。</p>
<p>​
ELU函数融合了Sigmoid和ReLU，左侧具有软饱和性，右侧无饱和性。右侧线性部分使得ELU能够缓解梯度消失，而左侧软饱能够让ELU对输入变化或噪声更鲁棒。ELU的输出均值接近于零，所以收敛速度更快。</p>
<h4 id="softplus">6.SoftPlus</h4>
<p>公式： <span class="math display">\[ f(x)=log(1+e^x) \]</span>
图像：</p>
<p><img data-src="/images/softplus.png" /></p>
]]></content>
      <categories>
        <category>深度学习</category>
      </categories>
      <tags>
        <tag>读书笔记</tag>
        <tag>深度学习</tag>
      </tags>
  </entry>
  <entry>
    <title>外文文献词汇</title>
    <url>/2022/03/07/%E5%A4%96%E6%96%87%E6%96%87%E7%8C%AE%E8%AF%8D%E6%B1%87/</url>
    <content><![CDATA[<h2 id="外文文献词汇">外文文献词汇</h2>
<hr />
<table>
<tbody>
<tr class="odd">
<td>decomposition</td>
<td style="text-align: left;">分解；变质</td>
</tr>
<tr class="even">
<td>demonstrate</td>
<td style="text-align: left;">证明；展示；论证</td>
</tr>
<tr class="odd">
<td>benchmark</td>
<td style="text-align: left;">基准；标杆</td>
</tr>
<tr class="even">
<td>tackle</td>
<td style="text-align: left;">应付；解决；处理</td>
</tr>
<tr class="odd">
<td>vice versa</td>
<td style="text-align: left;">反之亦然</td>
</tr>
<tr class="even">
<td>metric</td>
<td style="text-align: left;">衡量标准</td>
</tr>
<tr class="odd">
<td>heuristic</td>
<td style="text-align: left;">启发式的；探索的</td>
</tr>
<tr class="even">
<td>non-symmetric</td>
<td style="text-align: left;">不对称式的</td>
</tr>
<tr class="odd">
<td>paradox</td>
<td style="text-align: left;">悖论</td>
</tr>
<tr class="even">
<td>cardinality</td>
<td style="text-align: left;">基数；集的势</td>
</tr>
<tr class="odd">
<td>authoritative</td>
<td style="text-align: left;">可信的；权威的</td>
</tr>
<tr class="even">
<td>denominator</td>
<td style="text-align: left;">分母</td>
</tr>
<tr class="odd">
<td>trivial</td>
<td style="text-align: left;">不重要的；琐碎的</td>
</tr>
<tr class="even">
<td>optimal</td>
<td style="text-align: left;">最理想的；最佳的</td>
</tr>
<tr class="odd">
<td>numerator</td>
<td style="text-align: left;">分子</td>
</tr>
<tr class="even">
<td>corresponding</td>
<td style="text-align: left;">一致的</td>
</tr>
<tr class="odd">
<td>intersection</td>
<td style="text-align: left;">十字路口；交叉；交集</td>
</tr>
<tr class="even">
<td>ambiguity</td>
<td style="text-align: left;">模棱两可；暧昧</td>
</tr>
<tr class="odd">
<td>preliminary</td>
<td style="text-align: left;">初步的；准备的</td>
</tr>
<tr class="even">
<td>intuition</td>
<td style="text-align: left;">直觉</td>
</tr>
<tr class="odd">
<td>sufficient</td>
<td style="text-align: left;">足够的；充分的</td>
</tr>
<tr class="even">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="even">
<td></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>文献阅读</tag>
        <tag>English</tag>
      </tags>
  </entry>
</search>
